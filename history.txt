ESX Frisch installiert
ESX hat neue Systeme

ox_lib & ox_inventory installiert
dessen SQL's in die Datenbank geschrieben (kann auf der offizielen Website eingesehen werden)

17.08.2022
NativeUILua_Reloaded installiert.
myCarMileage installiert und SQL ausgeführt:
{
ALTER TABLE `owned_vehicles` 
ADD COLUMN `milage` DOUBLE NOT NULL DEFAULT 0;
}
myGarage installiert und SQL ausgeführt:
{
 -> siehe mygarage.sql
}